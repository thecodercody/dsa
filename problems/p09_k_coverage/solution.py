def solution(intervals: list[list[int]], k: int) -> list[list[int]]:
    # TODO: return all maximal contiguous segments covered by >= k intervals.
    # Hint: sweep line — for each [a, b], mark +1 at a and -1 at b+1.
    #       Sort event positions, walk through tracking count.
    #       Record a segment whenever count transitions across the k boundary.
    pass
